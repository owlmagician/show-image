export type MediaPack = {
    NAME:string,
    TYPE:string,
    PATH:string
}

export type UsbPack = {
    NAME:string,
    TYPE:string,
    BASE64:string
}
export type BarcodeStruct = {
    NAME: string,
    RESULT: boolean,
    NOTIFICATION: string
  }